# A22-voxcpm E12 P2: VRAM probe config (2 steps, batch 1, no accum, no saves).
pretrained_path: models/voxcpm2
train_manifest: overridden-per-probe
val_manifest: null
sample_rate: 16000
out_sample_rate: 48000
batch_size: 1
grad_accum_steps: 1
num_workers: 2
num_iters: 2
log_interval: 1
valid_interval: 1000000
save_interval: 1000000
learning_rate: 0.00001
weight_decay: 0.01
warmup_steps: 1
max_steps: 2
max_batch_tokens: 8192
max_grad_norm: 1.0
save_path: tmp/voxcpm/probe_ckpt
tensorboard: tmp/voxcpm/probe_ckpt/tb
lambdas:
  loss/diff: 1.0
  loss/stop: 1.0
